# Local development

Use Node 20 for this project.

```bash
nvm use
npm install
npm run check
npm run build
```

The production workflow also uses Node 20, so local validation should match CI as closely as possible.
